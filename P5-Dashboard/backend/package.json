{
	"name": "p5-dashboard-backend",
	"version": "1.0.0",
	"description": "Express backend for IoT Dashboard — polls ESP32 telemetry and broadcasts to React frontend via WebSocket",
	"main": "server.js",
	"scripts": {
		"start": "node server.js",
		"dev": "node --watch server.js"
	},
	"dependencies": {
		"cors": "^2.8.5",
		"express": "^4.21.2",
		"ws": "^8.18.0"
	}
}
